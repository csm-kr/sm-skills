# 프레젠테이션 기획·제작 가이드 (Planner + Designer)

pt-maker가 덱을 만들 때 따르는 품질 기준. 2025 프레젠테이션 디자인 베스트 프랙티스(아래 References)를 종합했다. **덱을 만들기 전과 마감 전에 이 문서를 기준으로 점검한다.**

## 0. 두 역할로 일한다
- **기획자(Planner)** — 슬라이드를 만들기 *전에* 메시지·청중·스토리라인·구조를 잡는다.
- **디자이너(Designer)** — 한 슬라이드씩 시각 위계·여백·시각화로 마감한다.

둘을 분리하면 "예쁜데 메시지 없는" 덱과 "내용은 있는데 안 읽히는" 덱을 둘 다 피한다.

## ★ 심미성 체크리스트 (사용자 핵심 — 빌드·마감마다 슬라이드별로 점검)
> 사용자가 반복해서 강조한 기준. 빌드 중(Phase 4)과 마감 QA(Phase 6)에 **슬라이드별 스샷으로** 다음을 본다. 처방 상세는 §2·§3·§4.

1. **가독성** — 본문 `line-height ≥ 1.5`·불릿 간 여유(빽빽 금지). 글자가 도형·아이콘·다른 글자와 **겹치지 않게**(도형 안 라벨은 정중앙 `cx,cy`, 아니면 충분히 띄움). **연도·숫자·고유명사는 정자체**(손글씨 금지), 한글 라벨은 Pretendard(모노 금지). 일반 HTML 덱의 미감 기준은 **본문·불릿 28-30px, hard floor 26px, 보조 라벨 20-22px, 출처·fine-print 15-16px**. 큰 발표장/프로젝터용이면 본문을 32px 안팎으로 올린다. 그보다 작게 줄여야 맞는 슬라이드는 문장을 줄이거나 나눈다.
2. **통일성** — 번호·헤더 등 **반복 요소는 모든 슬라이드 같은 위치**(좌상단 `.s-head` 고정). 같은 줄 박스·카드는 **높이 통일**(래퍼 flex column + 카드 `flex:1`). 색·폰트, 그리고 **.html = .pdf** 결과 일치(필요시 `export_pdf_shots.py`).
3. **균형** — 요소가 **한쪽으로 쏠리지 않게**(좌우·중심 균형). 화살표·말풍선 꼬리 등 '가리키는' 요소는 **대상(컵 등)에 실제로 닿고 정렬**되게.
4. **여백** — 휑하게 두지 않기. 남는 곳은 저채도 장식(커피 링·원두 `opacity .1~.18`)·손글씨 한 줄로 **의미 있게**(슬라이드당 1~2개, 과용 금지).
5. **시각자료** — 표지·챕터 오프너·핵심 개념·사례·근거·비교·프로세스·마감 등 주요 슬라이드는 사진/스샷/차트/SVG/승인된 AI 일러스트 중 하나를 반드시 갖는다. 콘텐츠 슬라이드가 2장 이상 연속 텍스트만이면 실패.
6. **다양성** — 모든 슬라이드 같은 레이아웃이면 단조로움 → 카드·미러·허브·가로 타임라인·중앙 statement·이미지 주연 등 **한 덱에 3~4종**.
7. **디테일(손글씨)** — 손글씨(Nanum Pen)는 **가벼운 메모·감탄에만** 따뜻한 포인트로. 사실 정보엔 쓰지 않는다(1번과 충돌).

> 운영(심미성을 받치는 프로세스): 이미지는 **플레이스홀더 → 비용 안내 → 승인 후 생성**(§3), 산출물 파일명은 **주제+버전**(`<주제>v<N>`).

## 1. 기획 원칙 (Planner)
1. **한 줄 메시지** — 발표 전체를 관통하는 한 문장. 모든 슬라이드가 이걸 지지하는지 본다.
2. **청중·목적** — 누구에게 무엇을 남길지 → 용어 깊이·톤 결정.
3. **스토리라인** — Why(도입) → What(전개) → So-what(종합). 길면 Chapter로 분할.
4. **One idea per slide** — 한 장에 한 주장. **제목에 "그리고/및"이 들어가면 두 장으로 쪼갠다.**
5. **3초 룰** — 청중이 3초 안에 슬라이드 핵심을 못 잡으면 아직 안 된 것.
6. **근거를 붙인다** — 주장마다 증거(실제 스샷·데이터·사례). 추상 명사만 X.
7. **1슬라이드 ≈ 1분** — 압축. 데이터는 슬라이드당 핵심 수치 하나.

## 2. 디자인 원칙 (Designer)
1. **시각 위계** — 가장 중요한 게 가장 크고 진하게. 시선 동선: 제목 → 핵심 → 근거.
2. **6×6 규칙** — 불릿 6개 이하, 줄당 6단어 이하. 문장 아닌 구(句).
3. **여백이 디자인** — 꽉 채우지 않는다(white space).
4. **dominant visual 1개** — 주요 슬라이드는 사진/스샷/차트/SVG/다이어그램/승인된 AI 일러스트 중 하나를 주인공으로 둔다. 보조 텍스트는 그 비주얼을 설명하게 한다.
5. **시각화 > 글** — 비교=표/카드, 흐름=화살표, 양=그래프, 데이터=차트.
6. **대비·일관성** — 밝은 배경+어두운 글. 폰트 크기·색을 슬라이드마다 일관되게.
7. **색은 의미로** — 기본 accent(로즈)는 강조 1~2개에만. 사용자가 색상 변경을 요청하면 Color Hunt를 palette source로 삼아 새 토큰을 구성하되, 가독성 대비와 일관성을 먼저 본다.
8. **전환 절제** — 부드럽고 목적 있게(fade fast).
9. **제목의 약속 = 본문의 비주얼** — 제목이 곡선·그래프·비교·그림을 말하면 본문에 그 시각물을 **실제로 그린다**. 예: "한 가지 곡선이 보였다" → 막대 3개가 아니라 진짜 곡선(SVG)을. 추상 막대·플레이스홀더로 때우면 약속 불이행 + 슬라이드가 비어 보인다.
10. **statement도 여백을 채운다** — 큰 한마디 슬라이드(s-statement)는 제목만 두면 아래가 휑하다. 제목을 뒷받침하는 **미니 비주얼**(곡선·역할 막대·아이콘 행)을 붙여 화면 중앙~하단을 채운다.
11. **레이아웃 베리에이션** — 콘텐츠 슬라이드가 전부 '불릿 좌·비주얼 우'면 단조롭다. 레이아웃을 돌려 쓴다: ① 텍스트좌/비주얼우 ② 미러(비주얼좌/텍스트우) ③ 3-카드 그리드 ④ 가로 연결 타임라인(연도 뱃지) ⑤ 허브(중앙 비주얼→갈래) ⑥ 중앙정렬 statement+미니 다이어그램 ⑦ 이미지 주연(폴라로이드 큼). **한 덱에서 최소 3~4종 섞는다.**
12. **가독성** — 본문 `line-height ≥ 1.5`, 불릿 사이 간격 넉넉히(`margin ~.58em`). 한글 라벨(SVG 텍스트·kicker)은 **모노폰트 금지**(자간이 "정 권 교 체"처럼 한글을 깨뜨림) → Pretendard. 본문을 전부 32px 이상으로 키우면 덱이 투박해지므로 일반 HTML 덱은 28-30px를 먼저 쓴다. **정확한 연도·숫자·고유명사는 손글씨 폰트(Nanum Pen) 금지** — 손글씨는 가벼운 메모·감탄에만, 사실 정보는 정자체로 적는다.
13. **빈 공간을 의미 있게 꾸민다** — 짧은 콘텐츠로 하단/모서리가 비면, 저채도 장식(커피 링 스탬프·원두 더블릿 `opacity .1~.18`)이나 손글씨 한 줄로 채워 균형을 잡는다. 단 §5의 '의미 없는 남발'과 구분(슬라이드당 1~2개, 콘텐츠를 가리지 않게).

## 3. 이미지 정책 — "그림을 넣을지" 판단
- **원칙(사용자 핵심): 주요 슬라이드는 시각자료가 필수** — 표지·챕터 오프너·핵심 개념·사례/근거·비교·프로세스·마감 슬라이드는 사진/스샷/차트/SVG/승인된 AI 일러스트 중 하나를 반드시 갖는다. 우선순위: ① 내용에 맞는 **실사 사진**(자유 라이선스·공개 호스팅 썸네일·사용자 제공) → ② **스크린샷·차트·SVG 다이어그램** → ③ 마땅한 게 없으면 **생성**(아래 승인·비용 규칙 준수).
- **텍스트-only 연속 금지** — 콘텐츠 슬라이드가 두 장 이상 연속 텍스트만이면 실패다. 설명형이면 SVG/차트/타임라인으로, 무드형이면 실사진이나 승인받은 생성 일러스트로 보강한다.
- **사용자 원본**(스케치·필기·스샷): 원본 그대로. 생성·변형 금지(폴라로이드 프레임).
- **실제 결과물 스샷 > 클립아트**: 웹 프로젝트는 `browse`로 스샷 찍어 쓴다.
- **세로로 긴 이미지**(풀페이지 스샷·상세페이지): 폴라로이드 대신 `object-fit:cover; object-position:top` 썸네일 카드. 여러 개면 카드 그리드로 쇼케이스.
- **생성 일러스트는 보조**: 아래 둘 중 하나일 때만 1~2장 생성한다.
  - ① 비유/개념이 글보다 그림으로 더 빨리 전달된다 (예: "메멘토 같은 개발자").
  - ② 표지·섹션·전설처럼 텍스트만으로 허전하고 무드가 필요하다(이야기 중심 덱의 감성 후크 1장).
- **생성 전 반드시 확인** — 자동 생성 금지. 1차 초안은 슬라이드 안에 `🖼 AI로 그릴 그림입니다` 플레이스홀더로 자리만 잡고, **예상 장수·비용($0.03/장)을 알린 뒤 사용자 승인**을 받는다. 승인 후 `gen_image.py`로 생성해 플레이스홀더를 교체. (나머지 데이터·흐름은 비용 0인 인라인 SVG 미니비주얼 우선.)
- 생성 시 **현재 활성 팔레트 반영**: 기본값은 `warm cream paper, hand-drawn pen sketch, minimal, rose accent (#C73463), lots of empty space, flat`. Color Hunt 등으로 팔레트를 재구성한 덱은 `paper/background`, `accent`, `ink` 색을 프롬프트에 반영한다. `gen_image.py`.

## 3.5 Photo Crop And Real-Map Hard Gate

- 인물, 선수, 인터뷰이, 창업자, 아티스트 사진은 얼굴/머리/눈/입/턱/손에 든 핵심 물건/유니폼 이름이 잘리면 실패다. `object-fit: cover`는 최종 PDF에서 핵심 피사체가 온전히 보일 때만 쓴다.
- 제품, 음식, 로고, UI 캡처, 건물, 랜드마크 사진은 슬라이드가 이름 붙인 핵심 피사체를 알아볼 수 있어야 한다. 잘리면 `object-fit: contain`, `object-position`, 프레임 비율 변경, 이미지 교체, 또는 레이아웃 변경으로 고친다.
- 실제 국가/도시/여행/언어권/음식권/역사 지도는 직접 그린 SVG로 대체하지 않는다. 웹/공식/공개 라이선스 지도 베이스를 먼저 찾고, 그 위에 핀·루트·반투명 영역·라벨·콜아웃을 overlay로 그린다.
- 직접 그린 지도는 정확한 지도가 아니라 개념 스케치일 때만 허용하며, 슬라이드에 그렇게 보이도록 명시한다. 지리 정확도가 필요한 슬라이드에서 손그림 지도는 hard fail이다.

## 4. 마감 QA (내보내기 전)
- `browse`로 **슬라이드별 스샷** 확인(넘침·깨짐·정렬). 전환 캡처 잔상을 피하려면 먼저 `transition:'none'`으로 끈다.
  - **모든 슬라이드를 빠짐없이 캡처**할 땐 `Reveal.slide(N)` 직접 점프 대신 `Reveal.slide(0)`에서 시작해 `Reveal.next()`로 한 장씩 넘기며 캡처한다. (직접 점프는 일부 인덱스에서 present 섹션이 비는 일이 있어 백지 스샷이 나온다. 스샷이 비면 캡처가 깨진 것이지 슬라이드가 빈 게 아닐 수 있으니 파일 크기로 한 번 더 확인.)
  - 캡처마다 전환·폰트 로드를 기다리도록 0.3~0.5초 텀을 둔다(`wait --networkidle` 후 시작).
- **약속·여백 점검** — 슬라이드별 스샷에서 ① **제목이 약속한 시각물/내용을 본문이 실제로 보여주는가**(곡선이라 했으면 곡선이 있는가) ② **휑하게 비어 보이지 않는가**(특히 statement 슬라이드). 둘 중 하나라도 아니면 미니 비주얼을 보강한다(§2-9, §2-10).
- **시각자료 게이트** — 표지·챕터 오프너·핵심 개념·사례·근거·비교·프로세스·마감 슬라이드가 텍스트만이면 실패다. 콘텐츠 슬라이드가 두 장 이상 연속 텍스트만이면 SVG/차트/타임라인/사진/승인된 생성 일러스트 중 하나로 보강한다.
- 제목 kicker 번호 순서·오타·정렬 점검.
- **PDF 페이지 수 = 슬라이드 수** 확인.
- **PDF bleed 검증(필수)** — PDF 각 페이지 위·아래에 인접 슬라이드가 비치거나 콘텐츠가 잘리면 안 된다. `scripts/verify_pdf.py deck.pdf`로 전 페이지 콘택트 시트를 만들어 **캡처로 다** 확인한다(페이지 비율 16:9도 검사). 슬라이드가 많아질수록 심해지므로 장수 늘린 뒤엔 반드시 재검증.
  - **원인·처방(템플릿에 이미 반영됨):** reveal `print-pdf`는 ① 기본 `pdfPageHeightOffset: -1`이라 `.pdf-page`가 `@page`보다 1px 작아 페이지마다 누적 어긋남(다음/이전 슬라이드 비침) → **`pdfPageHeightOffset: 0`**, ② reveal 기본 `center: true`가 `.center` 플렉스 중앙정렬과 겹쳐 print에서 콘텐츠를 아래로 밀어 잘림 → **`center: false`**(세로 정렬은 `.center`/`.s-bullets .pad`가 직접 처리). 둘 다 `Reveal.initialize`에 박혀 있어야 한다.
- **SVG 라벨·카드 점검** — SVG 안 텍스트 라벨이 도형 밖으로 넘치거나 도형과 겹쳐 안 읽히지 않는가(라벨은 도형 중앙 `cx,cy`에 두거나 충분히 띄운다). 말풍선·화살표 등 '가리키는' 요소는 가리키는 대상에 실제로 붙는가. 그리드 카드들은 높이가 같은가(래퍼 `flex column` + 카드 `flex:1`).
- **.html ↔ .pdf 일치** — PDF는 주제 이름으로 저장한다(`deck.pdf` 금지). reveal `?print-pdf`가 화면(paper.css)보다 줄간격이 좁게 나오면 `export_pdf_shots.py`(HTML 스샷을 16:9로 합치기)로 만들어 화면과 1:1을 보장한다.
- **버전 올림** — 표지/푸터의 `vX.Y`를 변경할 때마다 증가시킨다.

## 5. 안티패턴 (AI slop — 피할 것)
- 모든 슬라이드가 같은 레이아웃 → 단조로움. 슬라이드 종류를 섞는다.
- 불릿 10개 덤프.
- 의미 없는 이모지·스톡 일러스트 남발.
- 추상 명사만("효율성·시너지") 구체 사례 없음.
- 제목이 라벨("개요")이고 주장이 아님.
- 표지·챕터 오프너·핵심 개념·사례·마감 슬라이드가 텍스트만 있음. 개요 단계부터 `visual plan`을 적고 실제 시각자료로 채운다.
- 세로로 긴 이미지를 가로 프레임에 욱여넣어 빈 공간이 남음.
- 제목은 곡선·그래프·그림을 약속했는데 본문엔 추상 막대·플레이스홀더만 → 약속 불이행, 슬라이드가 비어 보임(§2-9).
- **원형 번호·아이콘 뱃지가 옆 라벨 글자보다 커서 아래 제목/줄을 침범**하거나 시각적으로 과해 보임 → 뱃지 높이를 라벨 글자에 맞춰 작게(≈1.2~1.3em)·`line-height:1`, 컨테이너 `margin-bottom ≥ .5em` 확보. **빌드 후 스샷으로 수직 겹침·줄 간격을 반드시 확인**(반복 이슈).
- 카드·박스 안 텍스트가 한쪽으로 쏠리거나 줄마다 중심이 안 맞음 → 박스 안 정렬을 통일(같은 줄 카드는 높이·padding·정렬 기준 동일, 세로 가운데가 필요하면 flex center). 좌우 들쭉날쭉 금지(§2-2·§2-6).
- 박스/카드 안에서 **라벨(eyebrow)·번호 바로 밑에 제목이 딱 붙음(간격 0)** 또는 큰 제목 줄간격이 빡빡 → ❌ 가독성. 라벨↔제목 사이 `margin-bottom`(≥.5em) 확보, 큰 제목은 `line-height ≥ 1.25`. 박스 안 라벨–제목–본문이 **균형 있게 호흡**하도록(반복 이슈).
- 카드 썸네일 사진을 짧은 고정높이 `object-fit:cover`로 깔면 **피사체가 잘려 안 보임**(음식·인물 등) → 비율 맞춘 박스(`aspect-ratio:3/2` 등) + `object-fit:contain`(배경 흰색)으로 **전체를 보이게**(피사체 보장). 큰 히어로 사진도 얼굴·머리·핵심 사물이 잘리면 실패다.
- 실제 지도 슬라이드에서 국가 윤곽, 도시 위치, 언어권, 여행 루트를 직접 그린 SVG로 대체함 → 웹/공식 지도 베이스를 먼저 확보하고, 그 위에 overlay로 핀·선·영역을 그린다.

## References (웹, 2025)
- [Ten simple rules for effective presentation slides — PMC](https://pmc.ncbi.nlm.nih.gov/articles/PMC8638955/)
- [10 Essential Design Principles — Flashdocs](https://www.flashdocs.com/post/10-design-principles-every-slide-creator-should-know)
- [One Idea Per Slide](https://conversationsoncareers.com/2026/01/one-idea-per-slide-the-rule-that-will-change-your-presentations/)
- [10 Visual Hierarchy Principles — JEG Design](https://www.jegdesign.com/design-chat/10-visual-hierarchy-principles-for-designing-a-slide-deck/)
- [Slide Deck Design: 10 Principles — Deckary](https://deckary.com/blog/slide-deck-design-tips)
